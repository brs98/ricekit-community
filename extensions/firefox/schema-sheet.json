[
  {
    "namespace": "sheet",
    "description": "Register a single user-origin stylesheet globally from a file URI, hot-swappable.",
    "functions": [
      {
        "name": "loadGlobal",
        "type": "function",
        "description": "Unregister any previously-loaded global user sheet, then loadAndRegisterSheet at the given file URI. Applies to every open document (chrome + content) and every document opened afterwards.",
        "async": true,
        "parameters": [
          { "name": "fileUri", "type": "string" }
        ]
      }
    ]
  }
]
